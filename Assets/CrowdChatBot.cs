using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

namespace IStreamYouScream
{
    // [System.Serializable] public class Messages : List<string> { }
    // [System.Serializable] public class MessagesMap : Dictionary<UnityEvent, Messages> { }
    public class CrowdChatBot : MonoBehaviour
    {
        // public MessagesMap messages = new MessagesMap()
        // {
        //     { StoryEvents.Instance.OnGhostStunned, new Messages() },
        // }
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}